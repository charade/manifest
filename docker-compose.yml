services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
    volumes:
      - ./src:/app/src
  db_service:
    image: mysql

    ports:
      - '3307:3307'
    env_file:
      - .env
